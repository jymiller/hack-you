# Secrets — never commit real keys
.env
.env.*
!.env.example

# Security scan output — reports quote matched secrets verbatim
.opsera/
*-report.json
gitleaks-report.json
npm-audit.json

# Private session seed (prep/plan stays out of the published repo)
HANDOFF.md

# OS
.DS_Store

# Node
node_modules/
dist/
build/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.data/
